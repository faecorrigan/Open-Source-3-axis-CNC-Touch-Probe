// copy these sections into testCycle()  to try out particular features



/* write and then read from config. 
  Serial.println("testing serial write");
  report_flash_vars_to_serial();
  delay(100);
  write_current_flash_vars();
  configurationVariables.debounceDelay = 123;
  Serial.print("debounceDelayInMemory: ");
  Serial.println(configurationVariables.debounceDelay);

  read_current_flash_vars();
  Serial.print("debounceDelayRestored: ");
  Serial.println(configurationVariables.debounceDelay);
  delay(100000);
  //while(1);


*/


